<h1>タスク一覧</h1>

<% if @tasks.any? %>
  <table class="table table-striped">
    
    <thead>
        <tr>
          <div class="row">
            <div class="col-xs-12 col-sm-offset-2 col-sm-8 col-lg-offset-3 col-lg-6">
              <th>id</th>
              <th>ステータス</th>
              <th>タスク</th>
            </div>
          </div>
        </tr>
    </thead>
    <tbody>
      <% @tasks.each do |task| %>
        <tr>
          <td><%= link_to task.id, task %></td>
          <td><%= task.status %></td>
          <td><%= task.content %></td>
        </tr>
      <% end %>
    </tbody>
    </div>
    </div>
  </table>
  <div>
    <%= paginate @tasks %>
  </div>
<% end %>
  
  <%= link_to '新規タスク追加ページ', new_task_path, class: "btn btn-primary" %>
